terraform {
  backend "s3" {
    bucket         = "REPLACE_WITH_YOUR_UNIQUE_BUCKET_NAME"
    key            = "wg-bastion/terraform.tfstate"
    region         = "sa-east-1"
    encrypt        = true
    dynamodb_table = "wg-bastion-tf-lock"
  }
}
